import { Table } from 'common/Table';
import { FullPageWrapper } from 'layouts';

export function Venue(): JSX.Element {
  return (
    <FullPageWrapper>
      <Table />
    </FullPageWrapper>
  );
}
